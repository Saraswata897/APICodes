{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "JSONPlaceholder Post Mismatched Schema",
  "description": "Schema for a single post from JSONPlaceholder API with intentional data type mismatches for testing",
  "type": "object",
  "required": [
    "userId",
    "id",
    "title",
    "body"
  ],
  "properties": {
    "userId": {
      "type": "string",
      "description": "The ID of the user who created the post (mismatched to string)"
    },
    "id": {
      "type": "string",
      "description": "The unique ID of the post (mismatched to string)"
    },
    "title": {
      "type": "string",
      "description": "The title of the post"
    },
    "body": {
      "type": "string",
      "description": "The body content of the post"
    }
  },
  "additionalProperties": false
}
